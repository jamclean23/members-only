<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign up</title>
</head>

<body>
    <h2>Sign Up</h2>

    <form method="POST" action="/auth/add_user">
        <div>
            <label for="username">Username</label>
            <input type="text" id="username" name="username"/>
            <% const userTakenValResults = ejsHelpers.checkValidationErrors('usernameTaken', errors) %>
            <% if (userTakenValResults) { %>
                <span><%= userTakenValResults %></span>
            <% } %>
        </div>

        <div>
            <label for="password">Password</label>
            <input type="password" id="password" name="password"/>
            <% const validationResults = ejsHelpers.checkValidationErrors('password', errors) %>
            <% if (validationResults) { %>
                <span><%= validationResults %></span>
            <% } %>
        </div>

        <input type="submit"/>
    </form>
</body>

</html>